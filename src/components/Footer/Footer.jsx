import style from './Footer.module.css';

const Footer = () => {
  return (
    <div>
      <p className={`text-white ${style.Footer}`}>Proyecto 1 de react</p>
    </div>
  );
};

export default Footer;
